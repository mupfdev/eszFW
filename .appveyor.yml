image: ubuntu

before_build:
- sudo apt-get update
- sudo apt-get install -y libsdl2-dev libxml2-dev
- git submodule update --init --recursive

build_script:
- mkdir -p build
- cd build
- cmake -DUSE_CUTE_TILED=OFF ..
- make
- cmake -DUSE_CUTE_TILED=ON ..
- make
